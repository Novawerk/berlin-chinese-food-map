TEAM_ID=

PRODUCT_NAME=Berlin Chinese Food
PRODUCT_BUNDLE_IDENTIFIER=com.novawerk.berlinfoodmap

// Google Maps SDK key. Create one at console.cloud.google.com → APIs &
// Services → Credentials. Restrict by bundle id com.novawerk.berlinfoodmap.
// Read at runtime in iOSApp.swift via Bundle.main.infoDictionary["MAPS_API_KEY"].
MAPS_API_KEY=

// CURRENT_PROJECT_VERSION = Android versionCode; MARKETING_VERSION = versionName.
// Both auto-synced from gradle/libs.versions.toml by `./gradlew syncVersionToIos`
// (run automatically by .github/workflows/bump-version.yml on every push to main).
CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0.0
